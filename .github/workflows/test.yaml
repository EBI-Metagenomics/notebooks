name: Test

on:
  push:
    branches: '*'
  pull_request:
    branches: '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Free up disk space
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: false
        docker-images: true
        swap-storage: false

    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'

    - name: Download Shiny Proxy
      run: wget -O shiny-proxy/shinyproxy-3.2.4.jar https://www.shinyproxy.io/downloads/shinyproxy-3.2.4.jar

    - uses: actions/setup-node@v6
      with:
        node-version: '24'
    
    - name: Install test suite dependencies
      working-directory: ./tests
      run: npm install

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    - name: Build Python Docker image
      uses: docker/build-push-action@v7
      with:
        context: .
        file: ./images/python/Dockerfile
        load: true
        push: false
        tags: quay.io/microbiome-informatics/emg-notebooks-python.dev:latest

    - name: Build R Docker image
      uses: docker/build-push-action@v7
      with:
        context: .
        file: ./images/r/Dockerfile
        load: true
        push: false
        tags: quay.io/microbiome-informatics/emg-notebooks-r.dev:latest

    - name: Run tests
      working-directory: ./tests
      run: npm test

    - uses: actions/upload-artifact@v7
      if: always()
      with:
        name: launching-jupyter-lab-screenshot
        path: |
          tests/launching_jl.png
          tests/shiny_proxy_launched.png
          tests/jl_launched.png

    - name: Login to Quay.io
      uses: docker/login-action@v4
      with:
        registry: quay.io
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}

    - id: docker-tag
      uses: yuya-takeyama/docker-tag-from-github-ref-action@v1

    - name: Push branch images
      run: |
        docker tag quay.io/microbiome-informatics/emg-notebooks-python.dev:latest quay.io/microbiome-informatics/emg-notebooks-python.dev:${{ steps.docker-tag.outputs.tag }}
        docker tag quay.io/microbiome-informatics/emg-notebooks-r.dev:latest quay.io/microbiome-informatics/emg-notebooks-r.dev:${{ steps.docker-tag.outputs.tag }}
        docker push quay.io/microbiome-informatics/emg-notebooks-python.dev:${{ steps.docker-tag.outputs.tag }}
        docker push quay.io/microbiome-informatics/emg-notebooks-r.dev:${{ steps.docker-tag.outputs.tag }}
